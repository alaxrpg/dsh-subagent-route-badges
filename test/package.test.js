import test from 'node:test'; import assert from 'node:assert/strict'; import pkg from '../package.json' with { type: 'json' };
test('manifest is a light DSH plugin with dual bundle deps', () => { assert.equal(pkg.version, '0.1.1'); assert.equal(pkg.dsh.plugin, true); assert.ok(pkg.keywords.includes('dsh-plugin')); assert.equal(pkg.dependencies['react-dom'], '>=18.2.0'); assert.ok(pkg.dependencies['@deepseek-ai/dsh-client-runtime']); for (const range of Object.values(pkg.peerDependencies)) assert.match(range, /^>=/); });
